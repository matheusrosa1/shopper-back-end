version: "3.1"

services:
  # Serviço que irá rodar o node
  app-shopper-back-end:
    # Imagem base do container
    build: .
    # Nome do container para facilitar execução
    container_name: shopper-back-end
    # Substitui o comando padrão da imagem do node
    command: npm run start:dev # inicia a API quando container for iniciado
    # Restarta a imagem caso algo a faça parar
    restart: always
    # Lista de volumes (diretórios) mapeados de fora para dentro do container
    volumes:
      # Monta o diretório atual, com todos os dados do projeto, 
      # dentro do diretório /app
      - ./:/app
      - ./src:/app/src
    # Lista de serviços do qual este serviço depende
    depends_on:
      # Precisa do banco funcionando antes de subir o node
      - db
    # Lista de portas mapeadas de dentro para fora do container
    # na notação porta_de_fora:porta_de_dentro
    ports:
      # Expõe a porta padrão da aplicação: altere aqui caso use outra porta
      - 3001:3001
    # Configura as variáveis de ambiente dentro do container
    environment:
      - DB_USER=root
      - DB_PASSWORD=password
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=shopper-back-end

  # Serviço que irá rodar o banco de dados
  db:
    container_name: db
    image: postgres:15
    restart: always
    # Lista de portas mapeadas de dentro para fora do container
    # na notação porta_de_fora:porta_de_dentro
    ports:
      # Expõe a porta padrão do banco de dados
      - 5432:5432
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=shopper-back-end
